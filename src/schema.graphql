type Query {
  persons: [Person!]!
}

type Mutation {
  createPerson(cmd: CreatePersonCommand!): Person!
}

type Person {
  id: String!
  securitySocialNumber: String!
  nickname: String!
}

input CreatePersonCommand {
  ssn: String!
  nickname: String!
}
